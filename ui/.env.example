# Both have sane defaults (see vite.config.js / src/conductor.js) — only
# needed if the Conductor server runs on a non-default port, or the agent's
# pipeline name changes.
VITE_CONDUCTOR_SERVER_URL=http://localhost:8080
VITE_AGENT_NAME=pgrn_sortilin_screening_pipeline
